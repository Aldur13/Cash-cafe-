<Application xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:CashCafe.App.Converters"
             x:Class="CashCafe.App.App">
    <Application.Resources>
        <ResourceDictionary>
            <converters:MoneyConverter x:Key="Money" />
            <converters:BalanceBrushConverter x:Key="BalanceBrush" />
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
            <converters:NotEmptyToVisibilityConverter x:Key="NotEmptyToVisibility" />

            <SolidColorBrush x:Key="Ink" Color="#17201C" />
            <SolidColorBrush x:Key="Muted" Color="#5D6B63" />
            <SolidColorBrush x:Key="Line" Color="#DDE4DF" />
            <SolidColorBrush x:Key="Paper" Color="#F7F9F7" />
            <SolidColorBrush x:Key="Card" Color="#FFFFFF" />
            <SolidColorBrush x:Key="Accent" Color="#1F5D45" />
            <SolidColorBrush x:Key="Good" Color="#1F7A4D" />
            <SolidColorBrush x:Key="Warn" Color="#9A6412" />
            <SolidColorBrush x:Key="Bad" Color="#B3261E" />

            <Style TargetType="Window">
                <Setter Property="Background" Value="{StaticResource Paper}" />
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="15" />
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource Ink}" />
            </Style>

            <Style x:Key="Heading" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>

            <Style x:Key="Label" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource Muted}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Margin" Value="0,0,0,4" />
            </Style>

            <!-- Every control at the counter is at least 44px high: the café may end up on a
                 touch screen, and a queue is no place for precision aiming. -->
            <Style TargetType="Button">
                <Setter Property="MinHeight" Value="44" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="Background" Value="{StaticResource Card}" />
                <Setter Property="BorderBrush" Value="{StaticResource Line}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Cursor" Value="Hand" />
            </Style>

            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="{StaticResource Accent}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="MinHeight" Value="56" />
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{StaticResource Line}" />
                        <Setter Property="Foreground" Value="{StaticResource Muted}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="MinHeight" Value="44" />
                <Setter Property="Padding" Value="10,8" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="BorderBrush" Value="{StaticResource Line}" />
            </Style>

            <Style x:Key="CardPanel" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource Card}" />
                <Setter Property="BorderBrush" Value="{StaticResource Line}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="16" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
